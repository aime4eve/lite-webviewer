apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-service
  namespace: smartmoldguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: device-service
  template:
    metadata:
      labels:
        app: device-service
    spec:
      containers:
      - name: device-service
        image: smartmoldguard/device-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres:5432/device_db"
        - name: SPRING_DATASOURCE_USERNAME
          value: "smg_admin"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "smg_password_2025"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-service:8761/eureka/"
        - name: EUREKA_CLIENT_ENABLED
          value: "true"
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: device-service
  namespace: smartmoldguard
spec:
  selector:
    app: device-service
  ports:
  - port: 8081
    targetPort: 8081
