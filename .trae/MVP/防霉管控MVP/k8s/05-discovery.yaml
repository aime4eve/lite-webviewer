apiVersion: apps/v1
kind: Deployment
metadata:
  name: discovery-service
  namespace: smartmoldguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: discovery-service
  template:
    metadata:
      labels:
        app: discovery-service
    spec:
      containers:
      - name: discovery-service
        image: smartmoldguard/discovery-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: EUREKA_INSTANCE_HOSTNAME
          value: "discovery-service"
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "false"
        ports:
        - containerPort: 8761
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8761
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  namespace: smartmoldguard
spec:
  selector:
    app: discovery-service
  ports:
  - port: 8761
    targetPort: 8761
