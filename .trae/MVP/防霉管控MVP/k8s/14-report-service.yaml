apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: smartmoldguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
    spec:
      containers:
      - name: report-service
        image: smartmoldguard/report-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres:5432/report_db"
        - name: SPRING_DATASOURCE_USERNAME
          value: "smg_admin"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "smg_password_2025"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-service:8761/eureka/"
        - name: EUREKA_CLIENT_ENABLED
          value: "true"
        ports:
        - containerPort: 8087
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: smartmoldguard
spec:
  selector:
    app: report-service
  ports:
  - port: 8087
    targetPort: 8087
