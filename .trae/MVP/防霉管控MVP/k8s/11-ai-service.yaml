apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: smartmoldguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
      - name: ai-service
        image: smartmoldguard/ai-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres:5432/ai_db"
        - name: SPRING_DATASOURCE_USERNAME
          value: "smg_admin"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "smg_password_2025"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: INFLUX_URL
          value: "http://influxdb:8086"
        - name: INFLUX_TOKEN
          value: "smg_influx_token_2025"
        - name: INFLUX_ORG
          value: "smartmoldguard"
        - name: INFLUX_BUCKET
          value: "telemetry"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-service:8761/eureka/"
        - name: EUREKA_CLIENT_ENABLED
          value: "true"
        ports:
        - containerPort: 8083
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: smartmoldguard
spec:
  selector:
    app: ai-service
  ports:
  - port: 8083
    targetPort: 8083
