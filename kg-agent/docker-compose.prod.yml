version: '3.8'

services:
  # Nginx 反向代理
  nginx:
    image: nginx:1.25-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Redis 缓存和消息队列
  redis:
    image: redis:7.2-alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - backend

  # Elasticsearch 全文检索
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.14.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - backend

  # Etcd for Milvus
  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd_data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - backend

  # MinIO for Milvus
  minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - minio_data:/data
    command: minio server /data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - backend

  # Milvus 向量检索
  milvus-standalone:
    image: milvusdb/milvus:v2.3.3
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus_data:/var/lib/milvus
    restart: unless-stopped
    depends_on:
      - "etcd"
      - "minio"
    networks:
      - backend

  # Nebula Graph - Graph Service
  nebula-graphd:
    image: vesoft/nebula-graphd:v3.6.0
    environment:
      - TZ=UTC
      - META_SERVER_ADDRS=nebula-metad:9559
    restart: unless-stopped
    depends_on:
      - nebula-metad
      - nebula-storaged
    volumes:
      - nebula_graphd_data:/data/meta
    command: --meta_server_addrs=nebula-metad:9559 --local_ip=nebula-graphd --ws_ip=nebula-graphd --port=9669 --ws_http_port=19669
    networks:
      - backend

  # Nebula Graph - Meta Service
  nebula-metad:
    image: vesoft/nebula-metad:v3.6.0
    environment:
      - TZ=UTC
    restart: unless-stopped
    volumes:
      - nebula_metad_data:/data/meta
    command: --meta_server_addrs=nebula-metad:9559 --local_ip=nebula-metad --ws_ip=nebula-metad --port=9559 --ws_http_port=19559 --data_path=/data/meta
    networks:
      - backend

  # Nebula Graph - Storage Service
  nebula-storaged:
    image: vesoft/nebula-storaged:v3.6.0
    environment:
      - TZ=UTC
      - META_SERVER_ADDRS=nebula-metad:9559
    restart: unless-stopped
    depends_on:
      - nebula-metad
    volumes:
      - nebula_storaged_data:/data/meta
    command: --meta_server_addrs=nebula-metad:9559 --local_ip=nebula-storaged --ws_ip=nebula-storaged --port=9779 --ws_http_port=19779 --data_path=/data/meta
    networks:
      - backend

  # 应用服务
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    environment:
      - FLASK_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ES_HOST=http://elasticsearch:9200
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - NEBULA_HOST=nebula-graphd
      - NEBULA_PORT=9669
    volumes:
      - app_data:/app/data
      - app_logs:/app/logs
    restart: unless-stopped
    depends_on:
      - redis
      - elasticsearch
      - milvus-standalone
      - nebula-graphd
    networks:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Celery Worker
  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: celery -A app.celery_app worker --loglevel=info
    environment:
      - FLASK_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ES_HOST=http://elasticsearch:9200
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - NEBULA_HOST=nebula-graphd
      - NEBULA_PORT=9669
    volumes:
      - app_data:/app/data
      - app_logs:/app/logs
    restart: unless-stopped
    depends_on:
      - app
      - redis
    networks:
      - backend

  # Celery Beat
  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: celery -A app.celery_app beat --loglevel=info
    environment:
      - FLASK_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    restart: unless-stopped
    depends_on:
      - app
      - redis
    networks:
      - backend

volumes:
  nginx_logs:
  redis_data:
  es_data:
  etcd_data:
  minio_data:
  milvus_data:
  nebula_graphd_data:
  nebula_metad_data:
  nebula_storaged_data:
  app_data:
  app_logs:

networks:
  backend:
    driver: bridge
