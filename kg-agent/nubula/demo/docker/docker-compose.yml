networks:
  nebula-net:
    driver: bridge

services:
  # Nebula Graph - Meta Service
  nebula-metad:
    image: vesoft/nebula-metad:v3.6.0
    container_name: nebula-metad
    environment:
      - TZ=UTC
      - USER=root
    ports:
      - "9559:9559"
      - "19559:19559"
    volumes:
      - /data/nebula-docker-data/nebula-metad:/data/meta
      - /data/nebula-docker-data/nebula-metad/logs:/logs
    command:
      - --meta_server_addrs=nebula-metad:9559
      - --local_ip=nebula-metad
      - --ws_ip=nebula-metad
      - --port=9559
      - --ws_http_port=19559
      - --data_path=/data/meta
      - --log_dir=/logs
      - --v=0
      - --minloglevel=0
      - --default_parts_num=10
      - --default_replica_factor=1
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://nebula-metad:19559/status"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - nebula-net

  # Nebula Graph - Storage Service
  nebula-storaged:
    image: vesoft/nebula-storaged:v3.6.0
    container_name: nebula-storaged
    environment:
      - TZ=UTC
      - USER=root
    ports:
      - "9779:9779"
      - "19779:19779"
    volumes:
      - /data/nebula-docker-data/nebula-storaged:/data/meta
      - /data/nebula-docker-data/nebula-storaged/logs:/logs
    command:
      - --meta_server_addrs=nebula-metad:9559
      - --local_ip=nebula-storaged
      - --ws_ip=nebula-storaged
      - --port=9779
      - --ws_http_port=19779
      - --data_path=/data/meta
      - --log_dir=/logs
      - --v=0
      - --minloglevel=0
      - --heartbeat_interval_secs=10
    restart: on-failure
    depends_on:
      - nebula-metad
    healthcheck:
      test: ["CMD", "curl", "-f", "http://nebula-storaged:19779/status"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - nebula-net

  # Nebula Graph - Query Service
  nebula-graphd:
    image: vesoft/nebula-graphd:v3.6.0
    container_name: nebula-graphd
    environment:
      - TZ=UTC
      - USER=root
    ports:
      - "9669:9669"
      - "19669:19669"
    volumes:
      - /data/nebula-docker-data/nebula-graphd:/data/meta
      - /data/nebula-docker-data/nebula-graphd/logs:/logs
    command:
      - --meta_server_addrs=nebula-metad:9559
      - --local_ip=nebula-graphd
      - --ws_ip=nebula-graphd
      - --port=9669
      - --ws_http_port=19669
      - --log_dir=/logs
      - --v=0
      - --minloglevel=0
    restart: on-failure
    depends_on:
      - nebula-metad
      - nebula-storaged
    healthcheck:
      test: ["CMD", "curl", "-f", "http://nebula-graphd:19669/status"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - nebula-net


  # Nebula Studio - Web UI
  nebula-studio:
    image: vesoft/nebula-studio:v3.8.0
    environment:
      - API_ROOT_URL=http://nebula-graphd:9669
    ports:
      - "7001:7001"
    depends_on:
      - nebula-graphd
    networks:
      - nebula-net