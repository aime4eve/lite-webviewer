# 代办事项记录

## Next TODO
1、完善前端多目录配置界面功能，只需要按照新增的目录刷新ES索引即可
2、ES索引刷新，不能让前端等待后端处理完成，需要异步处理

## 任务：支持多目录文档检索
- [x] 1. 后端开发
- [x] 2. 前端开发
- [x] 3. 验证与测试

## 任务：异步扫描与前端优化
- [x] 1. 后端开发
- [x] 2. 前端开发
- [x] 3. 验证与测试

## 任务：搜索性能优化（防抖与结果截断）

### 1. 配置管理
- [x] **修改 `application.yml`**
    - 在 `app.search` 下新增 `max-local-results` 配置项（默认值：100）。
- [x] **修改 `ConfigService.java`**
    - 读取 `app.search.max-local-results` 配置。
- [x] **修改 `ConfigController.java`**
    - 新增接口 `/api/v1/config/search`，用于前端获取搜索相关配置（如最大结果数）。

### 2. 前端开发
- [x] **修改 `App.jsx`**
    - **获取配置**：组件加载时调用 `/api/v1/config/search` 获取最大结果数限制。
    - **引入防抖**：使用 `lodash.debounce` 包装搜索输入处理逻辑（建议延迟 300ms）。
    - **优化算法**：
        - 使用 `for` 循环替代 `filter().map()`。
        - 遍历过程中计数，一旦匹配数量达到配置的上限，立即终止循环。
    - **UI 反馈**：如果结果被截断，在搜索结果列表底部显示提示（例如：“仅显示前 100 条结果，请输入更精确的关键词”）。

### 3. 验证与测试
- [x] **验证配置生效**
    - 修改 `application.yml` 中的限制值（如改为 10），重启后端。
    - 前端搜索通用词（如 "t"），确认结果数量不超过 10 条。
- [x] **验证性能优化**
    - 快速输入文字，确认界面无明显卡顿（防抖生效）。
    - 搜索大量匹配文件，确认结果列表渲染迅速且无卡顿（结果截断生效）。
