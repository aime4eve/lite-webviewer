# 代办事项记录

## 任务：支持多目录文档检索

### 1. 后端开发
- [X] **修改 `FileScanServiceImpl.java`**
    - 更新 `scanRootDirectories` 方法，将 `rootDirs` 字符串按逗号分隔解析为 `List<String>`。
    - 处理路径前后的空格。
    - 确保 `scanDirectories` 被调用时传入的是包含所有配置目录的列表。
- [X] **更新 `application.yml` (可选)**
    - 更新 `app.scan.root-dirs` 的注释，说明支持逗号分隔的多个路径。

### 2. 前端开发
- [X] **修改 `App.jsx`**
    - 优化根目录配置的编辑界面。
    - 将 `Input` 组件改为 `TextArea` 或增加提示信息，明确支持输入多个目录（使用逗号分隔）。
    - 确保提交配置时，将多行或多项合并为逗号分隔的字符串发送给后端。

### 3. 验证与测试
- [X] **验证单目录扫描**
    - 配置单个目录，执行扫描，确保原有功能正常。
- [X] **验证多目录扫描**
    - 配置两个或更多目录，执行扫描。
    - 确认所有配置目录下的文件都能被检索和预览。
- [X] **ES索引验证**
    - 确认Elasticsearch索引中包含来自不同目录的文档。

### 4. 实施计划
1. 先修改后端逻辑，支持解析逗号分隔的路径。
2. 再修改前端界面，提升用户录入多目录的体验。
3. 最后进行联合调试和验证。
